benchmark:
  num_runs: 2
  num_samples: 10
  steps_to_test:
  - 1
  - 4
  - 16
  - 64
data:
  data_dir: data/mock_images
  image_size: 64
  num_mock_images: 50
metrics:
  compute_fid: true
  compute_lpips: true
  compute_ssim: true
model:
  attention_resolutions:
  - 16
  - 8
  channel_mult:
  - 1
  - 2
  - 4
  channels: 64
  dropout: 0.1
  num_res_blocks: 2
paths:
  checkpoints: checkpoints
  figures: results/figures
  results: results
training_base:
  batch_size: 8
  epochs: 5
  learning_rate: 0.0001
  num_timesteps: 1000
  save_every: 10
training_rectified:
  batch_size: 16
  epochs: 3
  learning_rate: 0.0001
  num_reflow_iterations: 1
  save_every: 10
